from setuptools import setup

setup(
    name="arm_reach",
    version="0.1",
    install_requires=[
        "gymnasium",
        "pybullet",
        "numpy",
        "torch",
    ],
    packages=[],
)
